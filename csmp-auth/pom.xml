<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.csmp</groupId>
        <artifactId>csmp</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>csmp-auth</artifactId>

    <description>
        csmp-auth 认证授权中心
    </description>

    <dependencies>

        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-nacos</artifactId>
        </dependency>

        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-captcha</artifactId>
        </dependency>

        <!-- csmp Common Security-->
        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-security</artifactId>
        </dependency>

        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-social</artifactId>
        </dependency>

        <!-- csmp Common Log -->
        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-log</artifactId>
        </dependency>

        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-doc</artifactId>
        </dependency>

        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-web</artifactId>
        </dependency>

        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-ratelimiter</artifactId>
        </dependency>

        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-encrypt</artifactId>
        </dependency>

        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-dubbo</artifactId>
        </dependency>

        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-seata</artifactId>
        </dependency>
        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-tenant</artifactId>
        </dependency>

        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-common-service-impl</artifactId>
        </dependency>

        <dependency>
            <groupId>com.csmp</groupId>
            <artifactId>csmp-api-resource</artifactId>
        </dependency>

        <!-- 自定义负载均衡(多团队开发使用) -->
<!--        <dependency>-->
<!--            <groupId>com.csmp</groupId>-->
<!--            <artifactId>csmp-common-loadbalancer</artifactId>-->
<!--        </dependency>-->

        <!-- ELK 日志收集 -->
<!--        <dependency>-->
<!--            <groupId>com.csmp</groupId>-->
<!--            <artifactId>csmp-common-logstash</artifactId>-->
<!--        </dependency>-->

        <!-- skywalking 日志收集 -->
<!--        <dependency>-->
<!--            <groupId>com.csmp</groupId>-->
<!--            <artifactId>csmp-common-skylog</artifactId>-->
<!--        </dependency>-->

        <!-- prometheus 监控 -->
<!--        <dependency>-->
<!--            <groupId>com.csmp</groupId>-->
<!--            <artifactId>csmp-common-prometheus</artifactId>-->
<!--        </dependency>-->

    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
