<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.csmp</groupId>
    <artifactId>csmp-api-bom</artifactId>
    <packaging>pom</packaging>
    <version>${revision}</version>

    <description>
        csmp-api-bom api依赖项
    </description>

    <properties>
        <revision>2.6.0</revision>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- 系统接口 -->
            <dependency>
                <groupId>com.csmp</groupId>
                <artifactId>csmp-api-system</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- 供应链接口 -->
            <dependency>
                <groupId>com.csmp</groupId>
                <artifactId>csmp-api-supply</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- 资源服务接口 -->
            <dependency>
                <groupId>com.csmp</groupId>
                <artifactId>csmp-api-resource</artifactId>
                <version>${revision}</version>
            </dependency>

            <!-- workflow接口 -->
            <dependency>
                <groupId>com.csmp</groupId>
                <artifactId>csmp-api-workflow</artifactId>
                <version>${revision}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>
</project>
